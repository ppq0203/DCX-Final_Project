<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.momento.subject.mapper.SubjectMapper">	<!-- xml파일을 사용할 mapper가 있는 경로 -->
<!-- Heidisql연결후 입력 저장-->
	
		<!-- SELECT문-->
	<select id="selectSubject" parameterType="project.momento.subject.dto.SubjectDto" resultType="arraylist">
		<![CDATA[
		SELECT 
			PK_SUBJECT_SEQ AS pkSubjectSeq,
			SUBJECT_NAME AS subjectName,
			SUBJECT_STATE AS subjectState,
			SUB_START_DT AS subStartDt,
			SUB_END_DT AS subEndDt,
			SUBJECT_TEATHER AS subjectTeather,
			SUBJECT_TEST AS subjectTest,
			SUBJECT_TEST_DT AS subjectTestDt,
			REGIST_ID AS registId,
			REGIST_DT AS registDt,
			UPDATE_ID AS updateId,
			UPDATE_DT AS updateDt
		FROM 
			SUBJECT
		WHERE 
			PK_SUBJECT_SEQ=#{pkSubjectSeq}
		]]>
	</select>
</mapper>