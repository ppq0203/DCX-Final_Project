<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.momento.answer.mapper.ANSWERMapper">	<!-- xml파일을 사용할 mapper가 있는 경로 -->
<!-- Heidisql연결후 입력 저장-->
	<!-- SELECT문 -->
	<select id="UserAnswer" resultType="project.momento.answer.dto.AnswerDto">
		<![CDATA[
			SELECT
				ANSWERU_SER AS UserAnswer,
				ANSWER AS Answer,
				ANSWER_OX AS AnsweROx,
				REGIST_ID AS RegistId,
				REGIST_DT AS RegistDt,
				UPDATE_ID AS UpdateId,
				UPDATE_DT AS UpdateDt
			FROM 
				USER_ANSWER
			WHERE
				PK_ANSWER_SEQ = #{pkAnswerSeq}
		]]>
	</select>
	
	<!-- INSERT문 -->
	<insert id="Answer" parameterType="project.momento.answer.dto.AnswerDto">
		<![CDATA[
			INSERT INTO ANSWER
			(
				PK_ANSWER_SEQ,
				PK_QUSTION_SEQ,
				PK_USER_SEQ,
				ANSWER_USER,
				ANSWER_Qn,
				ANSWER_OX,
				REGIST_ID,
				REGIST_DT,
				UPDATE_ID,
				UPDATE_DT
			) 
			VALUES
			(
				ifnull(#{pkAnswerSeq}, null),
				#{answerUser},
				#{answerQn}
				#{answerOx},
				#{registId},
				#{registDt},
				#{updateId},
				#{updateDt}
			)
			ON DUPLICATE KEY UPDATE
			ANSWER_OX = IF( ANSWER_USER = ANSWER_QN, 'O', 'X' )
			
		]]>
	</insert>
	
<!--	<select id="UserAnswer" parameterType="project.momento.useranswer.dto.User_AnswerDto">
		SELECT ANSWERU_SER, ANSWER
		FROM USER_ANWER 
		WHERE ANSWER_OX IN (SELECT ANSWER_USER, ANSWER
							FROM USER_ANWER
							WHERE ANSWER = #{ANWERU_SER} AND ANSWER != 'X')
		
	</select> -->
	
	<!-- DELETE문 -->
	<delete id="Answer" parameterType="project.momento.answer.dto.AnswerDto">
		<![CDATA[
			DELETE FROM ANSWER
			WHERE
				PK_ANSWER_SEQ = #{pkAnswerSeq}
		]]>
	</delete>
	
	<!-- UPDATE문 --> 
	<update id="Answer" parameterType="project.momento.answer.dto.AnswerDto">
		<![CDATA[
			UPDATE SET
				USER_ANSWER
			WHERE
				UPDATE_ID = #{updateId},
				UPDATE_DT = #{updateDt}
		]]>
	</update>
	
</mapper>